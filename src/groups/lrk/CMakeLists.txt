# CMakeLists.txt                                                     -*-CMake-*-

# The subdirectories for individual packges must be listed in dependency order
# where dependencies occur before dependents.
lark_add_package( "lrkp" )
lark_add_package( "lrku" )
